services:
  # === 1️⃣ Backend (Node.js + Express + Postgres) ===
  - type: web
    name: apollo-enrichment-backend
    runtime: node
    plan: free
    buildCommand: |
      npm install
    startCommand: |
      node server.js
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: ADMIN_EMAIL
        value: admin@tristone-partners.com
      - key: ADMIN_PASSWORD
        value: admin123
      - key: JWT_SECRET
        generateValue: true
      - key: ENCRYPTION_KEY
        generateValue: true
      - key: DATABASE_URL
        value: postgresql://users_4ux6_user:5GLd7EnpxSD8eEBGODUFjk5XIhyYTJdf@dpg-d45dmm8dl3ps738b63qg-a.singapore-postgres.render.com/users_4ux6

  # === 2️⃣ Frontend (Vite React App) ===
  - type: web
    name: apollo-enrichment-frontend
    runtime: static
    buildCommand: npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_BASE
        value: https://apollo-enrichment-backend.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
